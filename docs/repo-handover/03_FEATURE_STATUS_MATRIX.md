# 03_FEATURE_STATUS_MATRIX

| Feature / Module | Purpose | Main routes/files | Current status | What works | Missing / broken | Evidence / Risk Level |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Clinic Onboarding** | Allow new clinics to register | `/onboard`, `actions.ts:createClinic` | Fully implemented | Form submission, slug validation, clinic & default doctor creation | None | Verified via `page.tsx` and `OnboardForm.tsx`. **Risk: Low** |
| **Login / Auth** | Staff authentication and session creation | `/login`, `/api/auth/login`, `proxy.ts`, `session.ts` | Fully implemented | Supabase auth validation, role fetching, HMAC cookie generation, Middleware header injection | None | Verified via `proxy.ts` and `api/auth/login/route.ts`. **Risk: Low** |
| **Admin Dashboard** | Secondary oversight view, not core V1 flow | `/[slug]/admin/page.tsx`, `actions.ts:getAdminStats` | Implemented but not primary V1 surface | Fetching stats, calculating wait times, utilization, generating CSS bar charts | Not needed for the frozen V1 backbone and should stay out of the main workflow promise | Verified in `AdminDashboard`. **Risk: Low as secondary surface** |
| **Voice Intake** | Optional receptionist assist for faster intake | `/[slug]/intake`, `actions.ts:createAppointment`, `voiceAdapter.ts`, `api/intake-voice` | Implemented but fragile | Audio recording, chunking, final transcription, LLM extraction to `PatientIntakeDraft` | Highly reliant on external Sarvam APIs; manual intake must remain the default path | Verified via `processPatientVoiceInput`. Fallback mock data exists. **Risk: Medium** |
| **Queue Management** | Live view of waiting patients and status updates | `/[slug]/queue`, `QueueDisplay.tsx`, `actions.ts:callNextPatient` | Fully implemented | Real-time Supabase subscriptions, updating statuses, rendering waiting list | None | Verified via `QueueDisplay.tsx` realtime hooks. **Risk: Low** |
| **Consult / AI Scribe** | Doctor consultation interface with optional AI draft | `/[slug]/queue/[id]/consult`, `actions.ts:generateSoapNote` | Implemented but fragile | Manual diagnosis, prescription, follow-up, completion; optional transcript-driven SOAP draft | LLM extraction failures return generic errors and should not be part of the core V1 promise | Verified via `generateSoapNote` in `actions.ts`. **Risk: Medium** |
| **Patient Search** | Lookup existing patients by phone/name | `/[slug]/intake/PatientLookup.tsx`, `actions.ts:searchPatients` | Fully implemented | Database fuzzy search and numeric matching | None | Verified in `actions.ts`. **Risk: Low** |
| **Patient Portal** | Public view for patients to check token and history | `/[slug]/patient/[token]`, `HistoryCard.tsx`, `api/tts` | Fully implemented | Token resolution and rendering visit history | TTS relies on Sarvam; audio is secondary. Self-serve booking / issue-raising should not be treated as V1 commitments | Verified via patient portal routes. **Risk: Low** |
| **Token Generation** | Generating sequential daily tokens | `supabase/migrations/*`, `actions.ts:createAppointment` | Fully implemented | SQL function `next_token_number` handles concurrency safely at the DB level | None | Verified in `20260406000000_clinic_init.sql`. **Risk: Low** |
| **Settings** | Update doctor profile and hours | `/[slug]/settings`, `actions.ts:updateDoctorSettings` | Fully implemented | Form submission, JSONB updates to `working_hours` | None | Verified in `SettingsForm.tsx`. **Risk: Low** |
| **WhatsApp Messaging** | Secondary communication channel, not V1 backbone | `api/whatsapp/send`, `whatsappAdapter.ts` | Stubbed / Partially Wired | API route exists, payloads are validated, DB logs are created | Actual API delivery is mocked (no real WhatsApp provider integrated). Do not promise this in V1 | Verified via `whatsappAdapter.ts`. **Risk: Low (By design)** |
